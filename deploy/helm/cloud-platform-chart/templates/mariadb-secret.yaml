apiVersion: v1
kind: Secret
metadata:
  name: mariadb
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/managed-by: Helm
stringData:
  root-password: {{ .Values.global.mariadb.rootpassword }}
  password: {{ .Values.global.mariadb.password }}
  username: {{ .Values.global.mariadb.username }}
